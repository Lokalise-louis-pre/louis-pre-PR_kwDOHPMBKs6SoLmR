import { default as React } from '../../../../node_modules/react';
import { ModalProps } from '../Modal';
export declare const ModalShell: ({ children, opened, title, onClose, size, overflow, inPortal, className, }: ModalProps) => React.JSX.Element | null;
