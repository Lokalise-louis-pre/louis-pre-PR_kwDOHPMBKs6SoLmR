import { default as React, ReactNode } from '../../../node_modules/react';
type PopoverWrapperProps = {
    inPortal?: boolean;
    children?: ReactNode;
};
export declare const PopoverWrapper: ({ inPortal, children }: PopoverWrapperProps) => React.JSX.Element;
export {};
