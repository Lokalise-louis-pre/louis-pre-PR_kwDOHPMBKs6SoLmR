import { default as React } from '../../../../node_modules/react';
type RectangleProps = {
    rotation: number;
    begin: number;
};
export declare const Rectangle: ({ rotation, begin }: RectangleProps) => React.JSX.Element;
export {};
